# 核心数据与Excel处理
pandas>=2.2.0,<3.0.0      # 兼容 2.x 系列，避免 3.0 破坏性变更
numpy>=1.24.0,<2.0        # 放宽版本范围，避免未来 numpy 2.x 潜在问题
openpyxl>=3.1.0

# 数据可视化
plotly>=5.18.0
kaleido                     # 用于 plotly 导出静态图片
matplotlib>=3.7.0
seaborn>=0.12.0

# 机器学习
scikit-learn>=1.3.0

# Word报告生成
python-docx

# 其他依赖（以下为注释说明，无需修改）
# python-dateutil, six, tzdata, pytz 等会自动随 pandas 安装
# contourpy, cycler, fonttools, kiwisolver, pyparsing 等是 matplotlib/seaborn 的间接依赖